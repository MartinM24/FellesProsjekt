CREATE TABLE groups(
	
groupName	 	VARCHAR(30),
parentID		VARCHAR(30),
FOREIGN KEY (parentID)		REFERENCES groups(groupName) 		ON DELETE CASCADE
															ON UPDATE CASCADE,

PRIMARY KEY 		(groupName));